

/** \brief Le pide un caracter al usuario y valida que sea igual a uno de dos caracteres que recibe la funcion
 *
 * \param input Variable que guarda el valor del caracter ingresado por el usuario
 * \param message Mensaje para que el usuario sepa que debe ingresar
 * \param eMessage Mensaje de error en caso que no ingrese el caracter correcto con el primer mensaje
 * \param lowLimit Posible caracter queremos que ingrese el usuario
 * \param hiLimit Posible caracter queremos que ingrese el usuario
 * \return Void
 *
 */
void validarDosChar(char* input, char message[], char eMessage[], char lowLimit, char hiLimit);

